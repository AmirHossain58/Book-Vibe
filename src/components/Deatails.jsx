import React from 'react';
import BookDetail from './BooksDetails/BookDetail';

const Deatails = ({bookDetail}) => {
    console.log(bookDetail);
    return (
        <div>
            
        </div>
    );
};

export default Deatails;